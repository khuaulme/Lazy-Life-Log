<%include partials/header.ejs%>

<div class = "container">
    <header class = "jumbotron">
        <h1>Welcome to My Lazy Life Log</h1>
        <p>Pick your drink.</p>
        <p>
            <a class = "btn btn-primary btn-lrg" href="/drinks/new">Enter a New Drink</a>
        </p>

    </header>

    <div class="row text-center" style="display:flex; flex-wrap:wrap;">
            <% drinks.forEach(function(drink){ %>
               <div class="col-md-3 col-sm-6">
                   <div class = "thumbnail">
                       <img src="<%=drink.image%>" >
                       <div class="caption">
                           <h3><%=drink.name%></h3>
                       </div>
                       <p>
                           <%= drink.genre %>
                       </p>
                       <p>
                           <%= drink.dservings %> servings today.
                       </p>
                       <p>
                           <%= drink.wservings %> servings this week.
                       </p>
                       <form action="/drinks/<%=drink._id%>/inc?_method=PUT" method="POST">
                           <button class="btn btn-success" onclick="<%=drink.dservings++%>">Have a Glass!</button>
                       </form>

                   </div>
               </div>
            <% }); %>
        </div>
    <p><br>

        <a href="/" class="btn btn-warning">Go Back</a>
    </p>
    </div>





<%include partials/footer.ejs%>
